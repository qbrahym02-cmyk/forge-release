# Forge Desktop — صفحة التحميل الرسمية

<div dir="rtl">

## حمّل الآن الإصدار v4.0.0 — الأتمتة

**Forge Desktop** — وكيلاً عاماً يعمل على جهازك: يبرمج، يبني الألعاب بمحرك Godot، يتحكم بالمتصفح والتطبيقات كأنه بشري — **والآن يجدول مهامه بنفسه ويعمل وأنت نائم، ويملأ نماذجك ببياناتك المشفرة محلياً**.

### التحميل المباشر

| الملف | الوصف | الحجم التقريبي |
|-------|-------|-----------------|
| [`Forge-Setup-4.0.0.exe`](https://github.com/qbrahym02-cmyk/forge-release/releases/download/v4.0.0/Forge-Setup-4.0.0.exe) | **المثبّت الرسمي** — نقرة واحدة وينتهي | ~117 MB |

### خطوات التثبيت (Windows)

1. حمّل ملف `Forge-Setup-4.0.0.exe` من الجدول أعلاه
2. انقر نقراً مزدوجاً على الملف → المثبّت يعمل تلقائياً (oneClick)
3. شغّل البرنامج من قائمة ابدأ → **Forge**
4. سجّل الدخول بحساب Forge (نفس حساب الموقع) — **ستجد محادثاتك وموصلاتك من الويب بانتظارك**

### ما الجديد في v4.0.0؟

- 🔔 **المهام المجدولة (Agent Cron)** — الوكيل الذي يعمل وأنت نائم: قل له «كل يوم الساعة 8 سجّل مبيعات الأمس في ملف» فينشئ مهمة تعمل بنفسها في الخلفية — **حتى لو أُغلقت النافذة** (يبقى التطبيق في شريط النظام). النتيجة تصل إشعاراً من نظام التشغيل + محادثة مخصصة بها سجل تدقيق كامل لكل تشغيل (ماذا نفّذ، وماذا كتب، وماذا حدث). يدعم الكلام الطبيعي عربي/إنجليزي، وcron قياسي، وكل-N-دقيقة، ومرة واحدة، والوكيل نفسه يستطيع جدولة مهامه عبر أداة `schedule_create`.
- 🔐 **خزنة البيانات المشفرة** — بياناتك الشخصية (الاسم، البريد، الهاتف، العنوان، البطاقة) محفوظة **مشفرة AES-256-GCM على جهازك فقط** — لا سحابة، لا مزامنة، لا تغادر الجهاز أبداً. عند ملء أي نموذج: أداة `vault_autofill` تقرأ صفحة النموذج الحالية، تطابق الحقول بالمرادفات العربية والإنجليزية، وتملأ الكل دفعة واحدة في أجزاء من الثانية. كشف التلاعب تلقائي (GCM)، والقيم مخفية في كل الواجهات وبطاقات الإذن.
- 📜 **سجل النشاط** — كل فعل نفّذه الوكيل على جهازك (كتابة ملف، أمر، تنزيل، تحكم ببرنامج، تعديل خزنة…) مسجل بطابع زمني في تبويب «كل ما فعله الوكيل» — انقر أي صف لتفتح جلسته وترى التفاصيل. شفافية كاملة تبني الثقة.
- 🧰 **54 أداة** (كانت 44): +6 أدوات خزنة (status/list/save/get/delete/autofill) و+4 أدوات جدولة (create/list/delete/run_now) — بصلاحيات مستقلة، ووضع التخطيط يمنع الوصول للخزنة والجدولة.

### ما الجديد في v3.9.0 (التاريخي)


- 🎮 **محرك الألعاب Godot مدمج — يعمل في الخلفية فقط**: اطلب من الوكيل «صنع لي لعبة» فيبنيها بمحرك Godot 4.3 حقيقي: يستورد المشروع ويفحص السكربتات ويشغّلها اختبارياً ويصدّرها — كل ذلك headless بلا أي نافذة محرر. تحصل على ملف اللعبة النهائي: **exe واحد لويندوز** (المشروع مدمج فيه)، نسخة لينكس، نسخة تعمل في المتصفح، ونسخة macOS. أول طلب ينزّل المحرك تلقائياً بشريط تقدم (~50MB) + قوالب التصدير مرة واحدة (~1.1GB).
- 🏗️ **سير عمل ألعاب مضبوط**: الوكيل ينشئ مشروعاً صالحاً (قوالب 2D بلاعب وقفز وجاذبية، 3D بكاميرا، أو فارغ)، يكتب المشاهد والمنطق بلغة GDScript، يتحقق بـ godot_check (كل خطأ بالملف والسطر)، يصلح ويكرر، ثم يبني ويسلّم — وتضغط تشغيل لتجرب لعبتك مباشرة.
- 📝 **ملء النماذج بسرعة ×10** — `browser_fill_form`: نموذج كامل في استدعاء واحد بدلالة أسماء الحقول البشرية (الاسم، البريد، كلمة المرور…) — يدعم القوائم المنسدلة (بنص الخيار)، مربعات الاختيار، حقول React، وينقر زر الإرسال بنقرة ماوس حقيقية.
- 🖥️ **تحكم أدق وأسرع بالتطبيقات**: `app_windows` يسرد النوافذ المفتوحة بمواقعها، `app_focus` يركّز النافذة المطلوبة قبل الكتابة (بدل الكتابة العمياء)، ووضع اللصق `paste` يكتب النصوص الطويلة فوراً عبر الحافظة.

### ما الجدد في v3.8.0 (التاريخي)

- 👆 **تحكم بالماوس والكيبورد على كل الجهاز** — nut.js يحقن أحداث إدخال حقيقية: الوكيل يشغّل أي برنامج (notepad، calc، أي لعبة…) ويتفاعل معه كأنه إنسان، والرؤية البصرية عند الحاجة فقط.

### ما الجديد في v3.7.0؟

- 🌙 **العمل في الخلفية** — إغلاق النافذة يخفي التطبيق في شريط النظام: مهام الوكيل والطرفية واختبارات المتصفح تستمر، قائمة الشريط تعرض المهام الجارية، ويصلك إشعار عند الاكتمال.
- 🌐 **متصفح الوكيل** 🤖👀 — الوكيل يفتح متصفحاً حقيقياً مرئياً ويتحكم به كأنه بشري: يقرأ الصفحة، ينقر (أحداث ماوس حقيقية تعمل على ألعاب canvas)، يكتب، يمرر، يصوّر لقطات داخل المشروع، وينفذ JS. اطلب منه اختبار أي موقع أو لعبة — أو مشروعك على localhost.
- 🔌 **موصلات Forge (نفس الويب)** — 29 موصلاً (GitHub، Gmail، Google Drive/Calendar/Sheets، Notion، Slack، Discord، Supabase، Stripe…) بتبويب «موصلات Forge»: ربط بنقرة عبر المتصفح أو بمفاتيح — والاتصال يعمل في الويب وسطح المكتب معاً.
- ☁️ **كل بيانات حسابك معك** — محادثاتك السحابية في الشريط الجانبي (قسم «سحابي») للقراءة والاستيراد كجلسة محلية (بلا استهلاك رصيد)، مع الموصلات والمكتبة والعملاء والرصيد.
- 🤖 **الوكيل يستعمل الموصلات السحابية فوراً** — مفاتيح الموصلات المرتبطة تُزامن لجهازه ويحقنها في نداءات API المطابقة تلقائياً.

### ما الجديد في v3.5.0؟

- 🖥️ **لوحة جانبية بنصف الشاشة** — المحادثة بجانب الكود/المعاينة/الطرفية في آنٍ واحد، بفاصل قابل للسحب وتمديد لكامل الشاشة.
- ▶️ **معاينة/تشغيل لكل لغات البرمجة** — HTML يُعاين حياً، وPython وNode وGo وC/C++ وRust وJava وShell تُشغّل بضغطة زر مع بث مباشر للمخرجات (السيرفرات تبقى حية مع زر إيقاف).
- ⌨️ **طرفية تفاعلية حقيقية** — نفّذ أوامرك داخل المشروع، وراقب أوامر الوكيل تظهر في الطرفية لحظة تنفيذها.
- 🔌 **صفحة الموصلات محلياً** — 11 موصلاً جاهزاً + موصل مخصص؛ المفاتيح تبقى على جهازك فقط، والوكيل يوثّق نداءات http_request بها تلقائياً.
- 🔑 **مفاتيح API من المحادثة → .env** — أعطِ الوكيل مفتاحاً فيخزنه في ملف `.env` ويُحقن في كل أمر تلقائياً.
- 🚀 **وكيل بلا حدود** — زر «تنفيذ كل شيء دون طلب إذن» في الإعدادات + 80 تكراراً للمهمة + تنزيل أي ملف/صورة/مكتبة (download_file) + نداء أي REST API (http_request).

### ما الجديد في v3.1.0؟

- 🔧 **إصلاح "fetch failed"** — كل اتصالات الشبكة عبر محرك Chromium الآن: بروكسي النظام وشهاداته وDNS كروم تُقرأ تلقائياً. إذا كان الموقع يعمل في متصفحك → البرنامج يعمل.
- 🎨 **الواجهة نسخة طبق الأصل من Forge Web** — نفس الألوان والخطوط والأبعاد: الرئيسية «ماذا تريد أن تبني؟» + الكومبوزر المتوهج + شرائح أنواع البناء + الشريط الجانبي بمجموعات اليوم/أمس/آخر 7 أيام.
- 🪜 **خطوات الوكيل لكل شيء** — حبوب مراحل ملونة (المخطط ← المبرمج ← المنتهي) تبقى بعد الإنهاء، بث مباشر لكل أداة مع المسارات والمدّات، بطاقات diff للمستخدم قبل/بعد.
- 🛡 **بطاقات الموافقة الذكية** — لم تعد تعلق بعد الرد عليها.

### ما الجديد في v3.0.0؟

- **ذكاء اصطناعي من سيرفر Forge** — نفس مزودات الويب (GLM 5.3 Flash وبدائله)، يتطلب حساباً
- **نظام الرصيد نفسه** — 50 رسالة عند التسجيل + 15 يومياً مجاناً | Pro عبر Stripe = بدون قيود
- **أدوات حقيقية على جهازك** — يقرأ ويكتب وينفّذ أوامر حقيقية في مجلد مشروعك، **بدون حد 300 ثانية** (حد السيرفرات على الويب)
- **تحديث تلقائي** — البرنامج يحدّث نفسه من هذا المستودع

</div>
